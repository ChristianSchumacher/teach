<%= form_for(@area, :html => {:multipart => true}) do |f| %>
  <% if @area.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@area.errors.count, "error") %> prohibited this area from being saved:</h2>

      <ul>
      <% @area.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name,'Name' %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :short_name,'Abt.Kuerzel' %><br>
    <%= f.text_field :short_name %>
  </div>
  <div class="field">
    <%= f.label :description,'Beschreibung' %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :file,'Schulungsunterlagen' %><br>
    <%= f.file_field :file %>
  </div>
  <div class="actions">
    <%= f.submit 'speichern'%>
  </div>
<% end %>
